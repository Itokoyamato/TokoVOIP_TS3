module.exports = {
  TSServer: "127.0.0.1", // IP Address of your teamspeak 3 server
  // WSServerIP: "127.0.0.1", // IP Address of the ws-server is running on, if not set, will auto discover the current host public IP
  WSServerPort: 3000, // Websocket port, default: 3000
  // FivemServerIP: "127.0.0.1", // IP Adress of your FiveM server, if not set, will auto discover the current host public IP
  // FivemServerPort: 32000, // Port of your FiveM Server, if not set, will try to contact FiveM server on same host
};
